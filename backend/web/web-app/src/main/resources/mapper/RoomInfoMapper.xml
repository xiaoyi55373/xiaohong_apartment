<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaohong.lease.web.app.mapper.RoomInfoMapper">

    <resultMap id="roomItemVoMap" type="com.xiaohong.lease.web.app.vo.room.RoomItemVo" autoMapping="true">
        <id property="id" column="id"/>
        <association property="apartmentInfo" javaType="com.xiaohong.lease.model.entity.ApartmentInfo" autoMapping="true">
            <id property="id" column="apartment_id"/>
        </association>
        <collection property="graphVoList" ofType="com.xiaohong.lease.web.app.vo.graph.GraphVo"
                    select="selectGraphListByRoomId" column="id" autoMapping="true"/>
        <collection property="labelInfoList" ofType="com.xiaohong.lease.model.entity.LabelInfo"
                    select="selectLabelListByRoomId" column="id" autoMapping="true"/>
    </resultMap>

    <select id="pageItem" resultMap="roomItemVoMap">
        select ri.id,
               ri.room_number,
               ri.rent,
               ai.id apartment_id,
               ai.name,
               ai.introduction,
               ai.district_id,
               ai.district_name,
               ai.city_id,
               ai.city_name,
               ai.province_id,
               ai.province_name,
               ai.address_detail,
               ai.latitude,
               ai.longitude,
               ai.phone,
               ai.is_release
        from room_info ri
                 left join apartment_info ai on ri.apartment_id = ai.id
        <where>
            ri.is_deleted = 0
            and ai.is_deleted = 0
            and ri.is_release = 1
            and ri.id not in
                (select room_id
                from lease_agreement
                where is_deleted = 0
                and status in (2, 5))

                <if test="queryVo.provinceId != null">
                    and ai.province_id = #{queryVo.provinceId}
                </if>
                <if test="queryVo.cityId != null">
                    and ai.city_id = #{queryVo.cityId}
                </if>
                <if test="queryVo.districtId != null">
                    and ai.district_id = #{queryVo.districtId}
                </if>
                <if test="queryVo.minRent != null and queryVo.maxRent != null">
                    and (ri.rent &gt;= #{queryVo.minRent} and ri.rent &lt;= #{queryVo.maxRent})
                </if>
                <if test="queryVo.paymentTypeId != null">
                    and ri.id in (
                    select
                    room_id
                    from room_payment_type
                    where is_deleted = 0
                    and payment_type_id = #{queryVo.paymentTypeId}
                    )
                </if>
                <if test="queryVo.minScore != null">
                    and ai.avg_score &gt;= #{queryVo.minScore}
                </if>
                <if test="queryVo.maxScore != null">
                    and ai.avg_score &lt;= #{queryVo.maxScore}
                </if>
        </where>
        <choose>
            <when test="queryVo.sortByScore == 'desc' or queryVo.sortByScore == 'asc'">
                order by ai.avg_score ${queryVo.sortByScore}
            </when>
            <when test="queryVo.orderType == 'desc' or queryVo.orderType == 'asc'">
                order by ri.rent ${queryVo.orderType}
            </when>
        </choose>
    </select>

    <select id="selectGraphListByRoomId" resultType="com.xiaohong.lease.web.app.vo.graph.GraphVo">
        select
            name,
            url
        from graph_info
        where is_deleted=0
        and item_type=2
        and item_id=#{id}
    </select>


    <select id="selectLabelListByRoomId" resultType="com.xiaohong.lease.model.entity.LabelInfo">
        select id,
               type,
               name
        from label_info
        where is_deleted = 0
          and id in
              (select label_id
               from room_label
               where is_deleted = 0
                 and room_id = #{id})
    </select>
    <select id="selectRoomById" resultType="com.xiaohong.lease.model.entity.RoomInfo">
        select id,
               room_number,
               rent,
               apartment_id,
               is_release,
               is_deleted
        from room_info
        where id = #{id}
    </select>

    <select id="pageItemByApartmentId" resultMap="roomItemVoMap">
        select ri.id,
               ri.room_number,
               ri.rent,
               ai.id apartment_id,
               ai.name,
               ai.introduction,
               ai.district_id,
               ai.district_name,
               ai.city_id,
               ai.city_name,
               ai.province_id,
               ai.province_name,
               ai.address_detail,
               ai.latitude,
               ai.longitude,
               ai.phone,
               ai.is_release
        from room_info ri
                 left join apartment_info ai on ri.apartment_id = ai.id
        where ri.apartment_id = #{id}
          and ri.is_deleted = 0
          and ai.is_deleted = 0
          and ri.is_release = 1
          and ri.id not in
              (select room_id
               from lease_agreement
               where is_deleted = 0
                 and status in (2, 5))
    </select>

</mapper>
