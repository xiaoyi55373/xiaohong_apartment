<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaohong.lease.web.app.mapper.BrowsingHistoryMapper">

    <resultMap id="historyItemVoMap" type="com.xiaohong.lease.web.app.vo.history.HistoryItemVo" autoMapping="true">
        <id property="id" column="id"/>
        <result property="roomId" column="room_id"/>
        <collection property="roomGraphVoList" ofType="com.xiaohong.lease.web.app.vo.graph.GraphVo"
                    select="selectGraphListByRoomId" column="room_id" autoMapping="true">
        </collection>
    </resultMap>

    <select id="pageItem" resultMap="historyItemVoMap">
        select bh.id,
               bh.browse_time,
               bh.user_id,
               ri.id   room_id,
               ri.room_number,
               ri.rent,
               ai.name apartment_name,
               ai.province_name,
               ai.city_name,
               ai.district_name
        from browsing_history bh
                 left join room_info ri on bh.room_id = ri.id
                 left join apartment_info ai on ri.apartment_id = ai.id
        where bh.is_deleted = 0
          and bh.user_id = #{userId}
        order by bh.browse_time desc
    </select>

    <select id="selectGraphListByRoomId" resultType="com.xiaohong.lease.web.app.vo.graph.GraphVo">
        select name,
               url
        from graph_info
        where is_deleted = 0
          and item_type = 2
          and item_id = #{room_id}
    </select>

    <!-- 查询热门房间（按浏览次数排序） -->
    <select id="selectHotRooms" resultType="java.util.HashMap">
        SELECT room_id,
               COUNT(*) as view_count
        FROM browsing_history
        WHERE is_deleted = 0
          AND browse_time >= CURRENT_TIMESTAMP - INTERVAL '30' DAY
        GROUP BY room_id
        ORDER BY view_count DESC
        LIMIT #{limit}
    </select>
</mapper>
