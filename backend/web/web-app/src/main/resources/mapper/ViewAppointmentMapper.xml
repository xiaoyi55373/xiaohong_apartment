<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaohong.lease.web.app.mapper.ViewAppointmentMapper">

    <resultMap id="appointmentItemMap" type="com.xiaohong.lease.web.app.vo.appointment.AppointmentItemVo" autoMapping="true">
        <id property="id" column="id"/>
        <collection property="graphVoList" ofType="com.xiaohong.lease.web.app.vo.graph.GraphVo" autoMapping="true"/>
    </resultMap>

    <select id="listItem" resultMap="appointmentItemMap">
        select
            va.id,
            ai.name apartment_name,
            gi.name,
            gi.url,
            va.appointment_time,
            va.appointment_status
        from view_appointment va
        left join apartment_info ai on va.apartment_id = ai.id
        left join graph_info gi on ai.id = gi.item_id and gi.item_type=1
        where va.is_deleted=0
        and gi.is_deleted=0
        and va.user_id=#{userId}
        order by va.create_time desc
    </select>
</mapper>
