<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaohong.lease.web.admin.mapper.SystemOperationLogMapper">

    <resultMap id="BaseResultMap" type="com.xiaohong.lease.model.entity.SystemOperationLog">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="business_type" property="businessType"/>
        <result column="method" property="method"/>
        <result column="request_method" property="requestMethod"/>
        <result column="operator_type" property="operatorType"/>
        <result column="oper_name" property="operName"/>
        <result column="user_id" property="userId"/>
        <result column="oper_url" property="operUrl"/>
        <result column="oper_ip" property="operIp"/>
        <result column="oper_param" property="operParam"/>
        <result column="json_result" property="jsonResult"/>
        <result column="status" property="status"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="oper_time" property="operTime"/>
        <result column="cost_time" property="costTime"/>
        <result column="operation_desc" property="operationDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, business_type, method, request_method, operator_type, oper_name, user_id,
        oper_url, oper_ip, oper_param, json_result, status, error_msg, oper_time, cost_time,
        operation_desc, create_time, update_time, is_deleted
    </sql>

    <select id="pageItem" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM system_operation_log
        <where>
            is_deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="operName != null and operName != ''">
                AND oper_name LIKE CONCAT('%', #{operName}, '%')
            </if>
            <if test="businessType != null">
                AND business_type = #{businessType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="createTimeBegin != null and createTimeBegin != ''">
                AND DATE_FORMAT(oper_time, '%Y-%m-%d') &gt;= #{createTimeBegin}
            </if>
            <if test="createTimeEnd != null and createTimeEnd != ''">
                AND DATE_FORMAT(oper_time, '%Y-%m-%d') &lt;= #{createTimeEnd}
            </if>
        </where>
        ORDER BY oper_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM system_operation_log
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectStatisticsByBusinessType" resultType="java.util.HashMap">
        SELECT 
            business_type as businessType,
            COUNT(*) as count
        FROM system_operation_log
        WHERE is_deleted = 0
        <if test="startTime != null">
            AND oper_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND oper_time &lt;= #{endTime}
        </if>
        GROUP BY business_type
    </select>

    <select id="selectStatisticsByStatus" resultType="java.util.HashMap">
        SELECT 
            status,
            COUNT(*) as count
        FROM system_operation_log
        WHERE is_deleted = 0
        <if test="startTime != null">
            AND oper_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND oper_time &lt;= #{endTime}
        </if>
        GROUP BY status
    </select>

    <select id="selectTrendByDate" resultType="java.util.HashMap">
        SELECT 
            DATE_FORMAT(oper_time, '%Y-%m-%d') as date,
            COUNT(*) as count
        FROM system_operation_log
        WHERE is_deleted = 0
        <if test="startTime != null">
            AND oper_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND oper_time &lt;= #{endTime}
        </if>
        GROUP BY DATE_FORMAT(oper_time, '%Y-%m-%d')
        ORDER BY date
    </select>

</mapper>
